---
tags: ["MR", "Terraform"]
ssids:
  - number: 1
    name: "cl2024-100-RedGuardian"
    auth_mode: "psk"
    encryption_mode: "wpa"
    wpa_encryption_mode: "WPA2 only"
    psk: "cl2024-LV"
    ip_assignment_mode: "Bridge mode"
    default_vlan_id: "100"
    adult_content_filtering_enabled: false
    use_vlan_tagging: true
    enabled: true
    lan_isolation_enabled: false

  - number: 2
    name: "cl2024-200-Yelena"
    auth_mode: "psk"
    encryption_mode: "wpa"
    wpa_encryption_mode: "WPA2 only"
    psk: "cl2024-LV"
    ip_assignment_mode: "Bridge mode"
    default_vlan_id: "200"
    adult_content_filtering_enabled: false
    use_vlan_tagging: true
    enabled: true
    lan_isolation_enabled: false